# Only allow installing package versions that have been published for at
# least this many days. Protects against supply-chain attacks where a
# compromised version is published and quickly used before it's caught
# and unpublished.
min-release-age=7
