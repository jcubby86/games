meta {
  name: Submit Story Entry
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/players/:uuid/story-entries
  body: json
  auth: bearer
}

params:path {
  uuid: {{playerUuid}}
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "value": "test"
  }
}
