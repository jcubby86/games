meta {
  name: PATCH /games/:uid
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/api/games/:uuid
  body: json
  auth: none
}

params:path {
  uuid: {{gameUuid}}
}

headers {
  Authorization: {{token}}
}

body:json {
  {
    "phase": "PLAY"
  }
}
